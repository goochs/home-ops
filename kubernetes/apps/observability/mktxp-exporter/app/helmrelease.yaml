---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/refs/heads/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app mktxp-exporter
spec:
  chartRef:
    kind: OCIRepository
    name: app-template
    namespace: flux-system
  interval: 1h
  values:
    controllers:
      mktxp-exporter:
        annotations:
          reloader.stakater.com/auto: "true"
        containers:
          app:
            args: ["--cfg-dir", "/config", "export"]
            command: ["mktxp"]
            env:
              TZ: America/New_York
            image:
              repository: ghcr.io/akpw/mktxp
              tag: 2.0.3@sha256:4c081729f8ca234675a4e99c431fb64d79c7ba2bc626d0703f1c154bdd4e0d4e
            resources:
              limits:
                memory: 512Mi
              requests:
                cpu: 500m
            securityContext:
              allowPrivilegeEscalation: false
              capabilities: {drop: ["ALL"]}
              readOnlyRootFilesystem: true
    defaultPodOptions:
      securityContext:
        runAsGroup: 1000
        runAsNonRoot: true
        runAsUser: 1000
    persistence:
      config:
        name: mktxp-exporter-secret
        type: secret
    service:
      app:
        controller: *app
        ports:
          http:
            port: 49090
    serviceMonitor:
      app:
        endpoints:
          - interval: 15s
            path: /metrics
            port: http
            scheme: http
            scrapeTimeout: 15s
        serviceName: *app
